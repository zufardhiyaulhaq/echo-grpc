syntax = "proto3";

option go_package = "github.com/zufardhiyaulhaq/echo-grpc/proto";

service Server {
    rpc GetReply(Message) returns (Response);
}

message Message {
    string message = 1;
}

message Response {
    string response = 1;
}
